{
    "name": "Verifile-workspace",
    "version": "1.0.0",
    "description": "Root workspace to run Verifile frontend and backend together",
    "scripts": {
        "dev:frontend": "cd verifile-frontend && npm run dev",
        "dev:backend": "cd DocsSignatureAppBE && sh ./mvnw spring-boot:run",
        "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
        "install:all": "cd verifile-frontend && npm install"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}